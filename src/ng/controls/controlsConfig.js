//hacked in for now to make ranges work
const conf = {
  controls: {
    ranges: {
      fromField: {
        name: '$gt'
      },
      toField: {
        name: '$lt'
      }
    }
  }
}

export default conf
