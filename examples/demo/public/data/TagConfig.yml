gridOptions:
  colModel:
    - { name: id, hidden: true }
    - { name: name, formatter: editActionLink }
    - { name: description, width: 100 }
  shrinkToFit: true
  # contextMenu: true
  pager: false
  toolbar:
    leftButtons:
      create: { class: 'hidden' }

editForm:
  name: { disabled: true}
  description: {}
